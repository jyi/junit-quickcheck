export JUNIT_QUICKCHECK_DIR=$( cd "$( dirname "${BASH_SOURCE[0]}" )" && pwd )
export CLASSPATH=$JUNIT_QUICKCHECK_DIR/generators/target/classes:$JUNIT_QUICKCHECK_DIR/core/target/classes:$CLASSPATH
export PATH=$PATH:$JQF_DIR/bin
